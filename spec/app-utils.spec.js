const { expect } = require('chai');
const utils = require('../app/utils/app-utils');

describe('renameKeys', () => {
  it('returns a new array without mutating the original array', () => {
    // expect(utils.renameKeys()).to.eql(something);
  });
});
